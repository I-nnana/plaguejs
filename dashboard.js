class Dashboard {
    constructor(_width, _height) {
        fill(200,200,200);
        rect(0,((_height * (100 - obj.dashboardHeight)) / 100), _width, ((_height * obj.dashboardHeight) / 100));
    }
}